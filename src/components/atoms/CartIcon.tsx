import * as React from 'react';

type CartIconProps = {
    cartQuantity: number,
}
 
const CartIcon: React.FC<CartIconProps> = ({cartQuantity}) => {
    return ( <div></div> );
}
 
export default CartIcon;